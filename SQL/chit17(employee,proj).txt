CREATE DATABASE COMPANY
USE COMPANY

CREATE TABLE EMPLOYEE (
E_ID INT PRIMARY KEY, 
E_NAME VARCHAR(50) NOT NULL, 
ADDRESS VARCHAR(30), 
SALARY INT,
COMMISSION INT
);
INSERT INTO Employee (E_ID, E_NAME, ADDRESS, SALARY, COMMISSION) VALUES
(1, 'Amit', 'Pune', 35000, 5000),
(2, 'Sneha', 'Pune', 25000, ), -- Assuming NULL or 0 for Sneha's commission since it's blank/implied 0 in the table
(3, 'Savita', 'Nasik', 28000, 2000),
(4, 'Pooja', 'Mumbai', 19000, ), -- Assuming NULL or 0 for Pooja's commission since it's blank/implied 0 in the table
(5, 'Sagar', 'Mumbai', 25000, 3000);

CREATE TABLE PROJECT (
    PR_NO INT PRIMARY KEY,
    ADDR VARCHAR(50)
);
INSERT INTO Project (PR_NO, ADDR) VALUES
(10, 'Mumbai'),
(20, 'Pune'),
(30, 'Jalgaon');

SELECT E_NAME,ADDRESS FROM Employee;

SELECT MAX(SALARY) AS MAX_SALARY,MIN(SALARY) AS MIN_SALARY FROM Employee;

SELECT E_NAME,SALARY FROM EMPLOYEE ORDER BY SALARY ASC;

SELECT E_NAME , salary FROM EMPLOYEE WHERE ADDRESS = "Pune" OR "Nashik";

SELECT * FROM EMPLOYEE;

SELECT E_NAME FROM EMPLOYEE WHERE COMMISSION = 0;

UPDATE EMPLOYEE 
SET address = "Nashik"
WHERE E_NAME = "Amit";

SELECT E_NAME, ADDRESS, SALARY, COMMISSION FROM EMPLOYEE WHERE E_NAME LIKE "A%";

SELECT COUNT(E_ID) AS SATFF_COUNT FROM EMPLOYEE WHERE ADDRESS = "Mumbai";

SELECT COUNT(E_NAME) AS STAFF_COUNT , ADDRESS FROM EMPLOYEE GROUP BY ADDRESS;

SELECT ADDRESS FROM EMPLOYEE union SELECT ADDR FROM PROJECT;

SELECT ADDRESS, MIN(SALARY) FROM EMPLOYEE GROUP BY ADDRESS;

SELECT ADDRESS, MAX(SALARY) FROM EMPLOYEE WHERE SALARY > 26000 GROUP BY ADDRESS;

DELETE FROM EMPLOYEE
WHERE SALARY > 30000;

SELECT * FROM EMPLOYEE